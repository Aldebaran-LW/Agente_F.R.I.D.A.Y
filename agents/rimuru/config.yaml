id: rimuru
name: Rimuru Tempest
role: Analista de Dados / Administrador de Consumo de Tokens

llm:
  provider: huggingface
  model: Qwen/Qwen2.5-7B-Instruct:fastest
  fallbacks:
    - groq/llama-3.3-70b-versatile
  # MIGRADO: ollama/smollm2:360m → HF (Ollama não existe no HF Space)

skills:
  - politica-seguranca
  - innovation-monitor

secrets:
  gateway: [OPENCLAW_GATEWAY_BASE_URL, OPENCLAW_AUTOMATION_TOKEN]
  optional: [TELEGRAM_ADMIN_CHAT_ID]
  # REMOVIDO: OPENROUTER_API_KEY (OpenRouter descontinuado no Jarvis/EC2)

providers:
  - deepseek
  - groq
  - huggingface
  # REMOVIDO: openrouter (descontinuado)

pipeline:
  watch:
    - gateway_vercel
    - hf_spaces
    - ec2_forge
    # REMOVIDO: openrouter_usage (OpenRouter descontinuado)
  alert_channel: telegram_via_jarvis
