id: ops
name: Heimdall
role: Vigia — GitHub org + deploy + Vercel (id tecnico: ops)

llm:
  provider: ollama
  model: smollm2:360m
  fallbacks: []

skills:
  - politica-seguranca
  - github-aldebaran
  - deploy-monitor
  - vercel-status

secrets:
  gateway: [OPENCLAW_GATEWAY_BASE_URL, OPENCLAW_AUTOMATION_TOKEN]
  optional: [GITHUB_TOKEN, VERCEL_API_TOKEN]
